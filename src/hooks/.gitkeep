# Git Directory Preservation File

This file ensures the `hooks` directory is tracked by Git, as Git does not track empty directories.

## Directory Purpose

The `hooks` directory contains all custom hooks and react hooks that handle client-side operations in the application.
